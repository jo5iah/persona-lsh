**/__pycache__/
**/ckpt/
**/persona_vectors/
**/wandb/
**/unsloth_compiled_cache
dataset/
eval_persona_extract/
eval_persona_eval/

# Environment variables and secrets
.env
.env.local
.env.*.local
*.key
*.pem

.venv/

# pytest cache
.pytest_cache/

# Demo runtime artifacts: track small result JSONs but never the large
# activation tensors. The .pt files are derivable by re-running the model;
# the JSON files (responses, judge scores, eliciting lists, analysis
# results) are the actual reproducible-results record.
demo/output/**/*.pt
